<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
    <f:entry title="${%Name}" field="name">
        <f:textbox/>
    </f:entry>
    <f:entry title="${%UUID}" field="uuid">
        <f:readOnlyTextbox default="UUID will be generated while saving"/>
    </f:entry>
</j:jelly>

